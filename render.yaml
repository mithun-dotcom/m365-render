services:
  - type: web
    name: m365-provisioner-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ALLOWED_ORIGIN
        value: "*"
      - key: PORT
        value: 10000
