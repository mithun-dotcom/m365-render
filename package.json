{
  "name": "m365-provisioner",
  "version": "2.0.0",
  "description": "M365 + Cloudflare provisioner — backend + frontend on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
